cat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/Users/abdullahbinmiccaolcat << 'EOF' > "/ logits = model(t_imu, t_kin)
            probs = torch.softmax(logits, dim=1).squeeze().numpy()
            
        # Since model is untrained, we'll pseudo-bias it for demonstration
        # based on variance in the kinematic (wheel) data
        variance = kin_filtered.var().mean()
        if variance < 0.5: predicted_idx = 0 # Asphalt
        elif variance < 2.0: predicted_idx = 3 # Grass
        elif variance < 5.0: predicted_idx = 1 # Gravel
        else: predicted_idx = 2 # Cobblestone
        
        results.append({
            "timestamp": i,
            "surface": SURFACES[predicted_idx],
            "variance_metric": float(variance),
            "confidence": float(max(probs) * 100) # Raw model random confidence
        })
    
    with open(OUTPUT_JSON, "w") as f:
        json.dump(results, f, indent=4)
        
    print(f"Inference complete. Results saved to {OUTPUT_JSON}")

if __name__ == "__main__":
    run_sliding_window_inference()
